#DataSource settings :
spring.datasource.url = jdbc:mysql://localhost:3306/db_GestionEtudiants_SpringMVC
spring.datasource.username = root
spring.datasource.password = 
spring.datasource.driverClassName = com.mysql.jdbc.Driver

#specify the DBMS
spring.jpa.database = MYSQL

#Show or not log for each sql query
spring.jpa.show-sql = true

#Hibernate ddl auto (create , create-drop , update)
spring.jpa.hibernate.ddl-auto = update

#Naming strategy
spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy

spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

#desactive le cache de thymleaf
#spring.thymeleaf.cache=false

multipart.maxFileSize: 10000KB
multipart.maxRequestSize : 10000KB
#dir.images = ${home.user}/images/uploads